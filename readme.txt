Алгоритм создания программы Индекса Массы Тела:
  1. На чём основывается Индекс Массы тела?
  Ответ: Формула для расчёта индекса массы тела: ИМТ = вес (кг) : рост² (м) 
  (Для вычислений нужно взять вес в килограммах и разделить на собственный рост в метрах, возведённый в квадрат. )
  Ответ.2: После получения результата от формулы мы сверяем полученный результат с диапазонами данных из таблицы ---
  --- Получаем итоговый результат.
  2. Задача: Пользователь вводит данные в консоль и получает результат.
  3. Как делаем?
  Ответ: 
  а) Формируем функцию с двумя параметрами вес и рост, используем формулу расчёта индекса массы тела и возвращаем итоговое значение, предварительно округлив его до 1 запятой
  б) Обьявляем переменную answer, которая вызывает функцию с нужными нам параметрами
  в) Полученый ответ фильтруется и оборачивается в красивый результат через условную инструкцию if..else
by Kohaku Nagadzima (X1r0s)

Algorithm for creating a Body Mass Index program:
  1. What is the basis for the Body Mass Index?
  Answer: The formula for calculating BMI is: BMI = weight (kg) : height² (m). 
  (To calculate, take your weight in kilograms and divide by your height in meters squared. )
  Answer.2: After getting the result from the formula, we check the obtained result with the data ranges from the table ---
  --- We get the final result.
  2. Task: User enters the data into the console and get the result.
  3. How do we do it?
  Answer: 
  a) Form a function with two parameters weight and height, use the formula for calculating BMI and return the final value, having rounded it up to 1 decimal point beforehand
  b) Declare the variable answer, which calls the function with the parameters we need.
  c) The received answer is filtered and wrapped in a beautiful result through the conditional instruction if..else.
by Kohaku Nagadzima (X1r0s)

